# EVAL_REPORT — 公开题库得分与前后对比

> **一句话**：starter 基线 **17.00 / 100** → 当前 **89.00 / 100**（均无 Key、mock 降级模式），
> 55 题全绿数 11 → 50；每一分的来龙去脉见 [`DEBUG_LOG.md`](DEBUG_LOG.md)。
> **live（真实模型）全量读数待补**：本仓库当前没有可用 Key（见 §五），已有部分 live 读数一并列出。

---

## 一、怎么复现这些分数

所有分数都出自同一份评测器 `eval/run_eval.py`（评测方提供，未改动），跑法：

```bash
# 终端 1：起服务（无 Key = mock 降级模式，llm_mode=mock）
cd starter
uv run python -m uvicorn kbqa.server:app --host 127.0.0.1 --port 8000

# 终端 2：跑公开题库（55 题 / 100 分）
cd ..
starter\.venv\Scripts\python.exe eval\run_eval.py ^
  --base-url http://127.0.0.1:8000 ^
  --questions eval\public_questions.jsonl ^
  --out <输出目录>
```

- live 模式：先按 [`LLM_SETUP.md`](LLM_SETUP.md) 设 `LLM_BASE_URL / LLM_API_KEY / LLM_MODEL`
  （**本报告不含 Key**），`/api/health` 的 `llm_mode` 变成 `live` 再跑，命令不变。
- 环境一致：Windows 11 + Python 3.12（`starter/.venv`）+ 随包数据（未改动）。
- **可比性指纹**（三条不全同的两次跑不可直接比）：`health.llm_mode`、
  `health.kb_chunks` + 索引缓存键（`starter/.cache/index.json` 的 `key` 字段）、`--only` 为空。
  下表每一行都满足三条全同（live 行除外，单独标注）。

---

## 二、得分总表：starter → 每个里程碑 → 最终

| # | 阶段（做了什么） | commit | 得分 | mode | Key | 报告 |
|---|---|---|---|---|---|---|
| 0 | **starter 基线**（原始代码） | `b93427b` | **17.00** / 100 | mock | 无 | 根目录 `report.md`（09-24） |
| 1 | 清洗口径 + 指标闭区间 + 索引缓存键 | `079344e` | 42.50 | mock | 无 | `notes/eval-full-2` |
| 2 | 中文二元组分词 + 多格式加载器 | `862e4c8` | 49.00（96 片段） | mock | 无 | `notes/eval-full-5` |
| 3 | 尾块不再丢弃（96 → 131 片段） | `525c120` | 48.00 | mock | 无 | `notes/eval-full-6` |
| 4 | 检索 doc_id 错位修复 | `f518db8` | **44.00（全程最低）** | mock | 无 | `notes/eval-full-8` |
| 5 | 版本键名统一 + `kb_docs` 口径 | `0d5663d` | 45.00 | mock | 无 | `notes/eval-full-9` |
| 6 | 版本过滤下沉到打分之前 | `384cccb` | 49.00 | mock | 无 | `notes/eval-full-10` |
| 7 | live 接入 + 模板回退不再拼整篇文档 | `fde0f29` | 51.00 | mock | 无 | `notes/eval-mock-d1-06` |
| 8 | 挑句排序方向修复（引用落到高分句） | `96577ca` | 56.00 | mock | 无 | `notes/eval-mock-d1-07` |
| 9 | 会话接线（history 进 planner + 分桶） | `1a57971` | **60.00** | mock | 无 | `notes/eval-mock-d1-04-45` |
| 10 | `run_sql` 只读 + 异常留痕（不涨分） | `a406b73` | 60.00 | mock | 无 | `notes/eval-full-12` |
| 11 | 三个安全判据接线 | `85320a7` | 66.00（safety 0→6） | mock | 无 | `notes/eval-full-13` |
| 12 | **planner 路由兜底修复（一步 +23）** | `f28f5ba` | **89.00** | mock | 无 | `notes/eval-full-14` |
| 13 | 安全判据收紧（②b~②e，防误伤） | `462e3bd` | 89.00（零翻转） | mock | 无 | `notes/eval-full-17` |
| 14 | live 收口 + 日期白名单 | `b288ad9` | 89.00（mock 零翻转） | mock | 无 | `notes/eval-full-18` |
| — | **最终（mock，无 Key）** | `b288ad9` | **89.00 / 100**，461 检查点通过 449 | mock | 无 | `notes/eval-full-18` |
| — | **最终（live，真实模型）** | 待补 | **待补** | live | **有** | 见 §五 |

说明：

- **#4 的 44.00 是全程最低，不是改坏了**。检索层修好之后，此前"靠检索不到 → 短拒答"
  侥幸通过的检查点成批失效（详见 [`DEBUG_LOG.md`](DEBUG_LOG.md) 的「由绿变红的检查点台账」）。
  中途从 49 掉到 44、又涨回来，是这份作业里最像"倒退"的一段——台账里逐条记了归因。
- #10 / #13 后半 / #14 在 mock 下不涨分：`run_sql` 只读是契约要求，
  安全判据收紧与 live 收口针对的是隐藏题库与真实模型行为（见 §六）。
- 早期报告（#1~#6）没有逐检查点计数，表中从 #9 起补齐（454/401 → 461/449）。

---

## 三、starter vs 最终：分类别对照（同一份评测器、同一份数据）

| 类别 | starter | 最终（mock） | 主要归因（详见 DEBUG_LOG 条目） |
|---|---|---|---|
| 指标接口 `metrics` | 1.00 / 6 | **6.00 / 6** | 区间改闭区间、五项公式按 KB-001 v3、零金额行剔除（条目 2） |
| 检索质量 `retrieval` | 6.00 / 15 | **14.00 / 15** | 中文二元组分词、加载器多格式、尾块不丢、doc_id 错位、版本过滤下沉（条目 3/4） |
| 纯数据 `data` | 0.00 / 12 | **12.00 / 12** | 清洗口径 + 指标口径（条目 2） |
| 文档 `doc` | 0.00 / 16 | **12.00 / 16** | 挑句排序方向（条目 7）+ planner 路由兜底（条目 11）；剩 4 分是挑句挑错具体事实 |
| 版本 `version` | 0.00 / 6 | **6.00 / 6** | 版本键名统一 + 路由兜底（被路由挡着，修完即满） |
| 混合 `hybrid` | 0.00 / 18 | **12.00 / 18** | 同 doc 路由；剩 6 分是"期望 hybrid 却给纯 doc"（未修） |
| 多轮 `multi_turn` | 1.00 / 9 | **9.00 / 9** | 会话接线（D1-08）+ 路由兜底解出 T03 |
| 拒答 `refusal` | 6.00 / 8 | **8.00 / 8** | 越界/越权口径补齐 |
| 安全 `safety` | 3.00 / 9 | **9.00 / 9** | 三个零调用判据接线（条目 10）+ 路由解出 S01 |
| 健康检查 `health` | 0.00 / 1 | **1.00 / 1** | `kb_docs` 改数入库文档数（条目 5） |
| **合计** | **17.00 / 100** | **89.00 / 100** | 剩 11 分：R04 1 · C02 2 · C04 2 · H01 3 · H06 3 |

## 四、健康接口前后对比（第一关口径）

| 项 | starter | 最终 | 说明 |
|---|---|---|---|
| `kb_docs` | 36（数的目录文件数） | **35**（数的入库文档数） | 33 md − README + 2 txt + 1 html |
| `kb_chunks` | 53（加载器只收 .md、尾块丢弃） | **131** | 条目 3/4 |
| `valid_sales_rows` | 18628（未清洗） | **18290** | 六条剔除 + 七字段去重（条目 2） |
| `llm_mode` | mock | mock / live | 无 Key 自动降级，契约 §7.2 |

## 五、live（真实模型）读数 —— **部分待补**

按作业要求，最终得分应当用配置了 Key 的状态跑。当前仓库与环境里**没有可用 Key**
（`LLM_SETUP.md` 与提交历史均不含 Key），因此：

- **已完成的真实模型读数**（配过 Key 的那一晚，`--only doc`，`llm_mode=live`）：

  | 代码状态 | commit | live `doc` | 说明 |
  |---|---|---|---|
  | 接入首日（三个 live 缺陷未修） | `dc66c37` | **8.00 / 16** | C01/C02/C05/C08 过 |
  | 模板回退约束 + 挑句排序修复后（同一份代码跑两次） | `96577ca` | **12.00 与 10.00 / 16** | ⚠️ 间隔 10 分钟的两次运行分别得 12 与 10，**不可复现**，见 §六.1 |

- **待补**：配 Key 后跑一次**全量**公开题库（命令同 §一，`llm_mode=live`），
  预期重点观察 C04（工具调用不收敛）、C06/C07（数字白名单）——
  这三处的修复（`b288ad9`）在 mock 下不可见，只有 live 能验证。
- **离线管道验证已完成**：用评测方自带的假模型（`eval/llm_gateway.py`）把
  `LLM_BASE_URL` 指向它，`llm_mode=live` 下 `--only doc` 8 题全部跑通、无崩溃，
  失败场景（`empty_content` / `bad_tool_args`）都给出结构化 refusal——
  证明 live 链路本身是通的；假模型回答与知识库无关，**该读数 0/16 无评分意义**。
- ★ **读数原则**：在工具调用收敛问题（机制 ⑪）确认修复之前，live 的 `doc` 分数
  不可复现，**不拿它当验收信号**；§六.1 写明了波动来源。

## 六、已知不可靠点与取舍（评审可能会问的都在这）

1. **live 的 `doc` 分数不可复现（±2）**：同一份代码两次跑分别得 10 与 12，
   差异集中在 C07（需要多轮 `search_kb` 才能答的题），来源是模型本身的随机性。
   修复 `b288ad9`（轮数上限 + 收口）之后预期收敛，但**尚未用真实模型复测**。
2. **`answer_type` 依赖模型遵守引用格式**：live 模式下文档题的判定依据是模型输出里的
   `[KB-xxx]` 标记；模型不输出标记就退化成 `data`（假模型离线验证时 8 题全是 `data`，
   即此现象）。提示词里已强制要求，但这是模型依从性风险，不是代码可以保证的。
3. **日期不参与数值白名单的代价**（README §四.8）：模型举例用的日期不再做真实性核对；
   换来"8 月 30 日的订单 9 月 1 日才退"这类合法示例不触发模板回退。
   数据库指标数字仍严格核对。
4. **安全判据的取舍**：写请求识别宁可误拒、不可误放（「更新过销售记录吗？」这类
   不带「的」的完成态问句会被拒答）。「清理 / 清了」不在写动词表里，
   「清理一下销售明细里的重复行」目前判不出写意图——有意保留的已知缺口。
5. **由绿变红不是回归**：检索层修好后，此前"靠检索不到 → 短拒答"侥幸通过的检查点
   成批失效（49 → 44 那一段），逐条归因见 [`DEBUG_LOG.md`](DEBUG_LOG.md) 的台账。
6. **没有为隐藏题库做任何针对性适配**：未写死任何数字、答案或文档内容；
   索引缓存键包含知识库路径与内容指纹，换库后重建即生效（条目 1）。

## 七、复现清单（按顺序执行即可逐行核对 §二）

```bash
git checkout b93427b   # starter 基线 → 17.00
# （起服务 + 跑评测，命令见 §一；下同）
git checkout 079344e   # 42.50
git checkout 862e4c8   # 49.00（96 片段）
git checkout 525c120   # 48.00（131 片段）
git checkout f518db8   # 44.00（全程最低，假绿消失）
git checkout 0d5663d   # 45.00
git checkout 384cccb   # 49.00
git checkout 96577ca   # 56.00
git checkout 1a57971   # 60.00
git checkout 85320a7   # 66.00
git checkout f28f5ba   # 89.00
git checkout main      # 最终 89.00（461 检查点 / 449 通过）
```

> 每次切换后**必须**执行重建命令（README §跑起来的步骤），
> 因为索引缓存键跟着知识库内容走，历史 commit 的缓存与当前代码不匹配。
> live 读数需要先按 `LLM_SETUP.md` 配置三个环境变量（不要提交 Key）。
